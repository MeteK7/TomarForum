@model TomarForumViewModel.ForumViewModels.ForumEditViewModel

@{
    Layout = "/Views/Shared/_AdminLayout.cshtml";
}

<div class="container-fluid">
    <ol class="breadcrumb mb-4">
        <li class="breadcrumb-item active">Edit</li>
    </ol>
    <div class="row">
        <div class="col-lg-6">
            <div class="card mb-4">
                <form asp-controller="Forum" asp-action="Edit" method="post" enctype="multipart/form-data">
                    <input asp-for="Id" readonly hidden />
                    <div class="card-header">
                        <i class="fas fa-pen-fancy mr-1"></i>
                        Edit this article.
                    </div>
                    <div class="card-body">
                        <div class="form-group">
                            <label asp-for="@Model.Title"></label>
                            <input asp-for="@Model.Title" class="form-control" required />
                            <span asp-validation-for="@Model.Title" class="text-danger"></span>
                        </div>
                        <div class="form-group">
                            <label asp-for="Description"></label>
                            <textarea asp-for="Description" class="form-control textarea" rows="8"></textarea>
                            <span asp-validation-for="Description" class="text-danger"></span>
                        </div>
                    </div>
                    <div class="card-footer">
                        <div class="form-group">
                            <button class="btn btn-primary" type="submit">Save</button>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>